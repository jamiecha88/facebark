{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "start": "cd client && npm start",
    "build": "cd client && npm run build",
    "eject": "cd client && npm run eject",
    "test": "jest"
  },
  "dependencies": {
    "react": "^16.8.0 || ^17.0.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  }
}
