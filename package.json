{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "start": "cd client && npm start",
    "build": "cd client && npm run build",
    "eject": "cd client && npm run eject",
    "test": "jest"
  },
  "dependencies": {
  },
  "devDependencies": {
    "jest": "^27.0.0"
  }
}
