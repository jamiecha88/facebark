{
  "name": "facebark",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build",
    "eject": "cd client && npm run eject",
    "start": "cd client && npm start",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^27.0.0"
  }
}
